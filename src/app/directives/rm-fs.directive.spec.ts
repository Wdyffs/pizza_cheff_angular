import { RmFsDirective } from './rm-fs.directive';

describe('RmFsDirective', () => {
  it('should create an instance', () => {
    const directive = new RmFsDirective();
    expect(directive).toBeTruthy();
  });
});
