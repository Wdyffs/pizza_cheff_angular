import { RestrictDotsDirective } from './restrict-dots.directive';

describe('RestrictDotsDirective', () => {
  it('should create an instance', () => {
    const directive = new RestrictDotsDirective();
    expect(directive).toBeTruthy();
  });
});
